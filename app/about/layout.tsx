import type { Metadata } from "next";

export const metadata: Metadata = { title: "About the Work | Brian McKeon Painting", description: "Learn about the preparation, communication, and care behind every Brian McKeon Painting project." };

export default function AboutLayout({ children }: { children: React.ReactNode }) { return children; }
