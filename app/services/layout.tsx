import type { Metadata } from "next";

export const metadata: Metadata = { title: "Painting Services | Brian McKeon Painting", description: "Interior, exterior, deck, fence, commercial, furniture, and specialty painting services." };

export default function ServicesLayout({ children }: { children: React.ReactNode }) { return children; }
