import type { Metadata } from "next";

export const metadata: Metadata = { title: "Completed Work | Brian McKeon Painting", description: "Explore real completed exterior, outdoor, and interior painting projects." };

export default function WorkLayout({ children }: { children: React.ReactNode }) { return children; }
