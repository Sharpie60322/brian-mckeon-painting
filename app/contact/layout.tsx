import type { Metadata } from "next";

export const metadata: Metadata = { title: "Request an Estimate | Brian McKeon Painting", description: "Request a painting estimate or contact Brian McKeon Painting by phone or email." };

export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
